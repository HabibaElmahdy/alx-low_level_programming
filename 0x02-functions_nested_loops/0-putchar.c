#include "main.h"
/**
 * main - Print _putchar
 * Description: prints _putchar
 * Return: Always 0
 */
int main(void)
{
char str[] = "_putchar";
int i = 0;

while (str[i] != 0)
{
char c = str[i];

putchar(c);
i++;
}
putchar(n);
return (0);
}
